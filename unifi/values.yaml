unifi:
  ingress:
    main:
      enabled: true
      annotations:
        ingress.kubernetes.io/protocol: https
      hosts:
        - host: unifi.lucyscrib.com
          paths:
            - path: /
              pathType: Prefix

  mongodb:
    enabled: true
    persistence:
      enabled: true
      size: 2Gi
